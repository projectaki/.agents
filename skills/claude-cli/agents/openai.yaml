interface:
  display_name: "Claude CLI"
  short_description: "Stream one observable Claude CLI task"
  default_prompt: "Use $claude-cli to run one observable read-only Claude task in a subagent and stream its progress."
policy:
  allow_implicit_invocation: false
