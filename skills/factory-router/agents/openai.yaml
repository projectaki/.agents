interface:
  display_name: "Factory Lifecycle Router"
  short_description: "Choose and briefly explain the next stage"
  default_prompt: "Use $factory-router to choose and briefly explain the next lifecycle stage from the current task context."
policy:
  allow_implicit_invocation: false
