interface:
  display_name: "Factory Intake Context"
  short_description: "Clarify task and gather context"
  default_prompt: "Use $factory-context to clarify a task and gather implementation context."
policy:
  allow_implicit_invocation: false
