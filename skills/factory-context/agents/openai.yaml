interface:
  display_name: "Factory Intake Context"
  short_description: "Gather complete evidence before planning"
  default_prompt: "Use $factory-context to gather the issue, codebase, and documentation context needed before planning."
policy:
  allow_implicit_invocation: false
