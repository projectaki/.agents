interface:
  display_name: "Factory Handoff"
  short_description: "Persist and resume lifecycle checkpoints"
  default_prompt: "Use $factory-handoff to persist or resume this lifecycle at its deterministic task path."
