interface:
  display_name: "Factory Handoff"
  short_description: "Persist compact Factory task state"
  default_prompt: "Use $factory-handoff to persist or resume the compact Factory task records."
policy:
  allow_implicit_invocation: false
