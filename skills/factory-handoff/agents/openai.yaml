interface:
  display_name: "Factory Handoff"
  short_description: "Persist, observe, and resume lifecycle checkpoints"
  default_prompt: "Use $factory-handoff to persist an immutable lifecycle checkpoint or resume the current task state."
