interface:
  display_name: "Factory Handoff"
  short_description: "Persist and inspect lifecycle run history"
  default_prompt: "Use $factory-handoff to persist or inspect lifecycle checkpoints, route reasoning, and the current task state."
