interface:
  display_name: "Factory Evidence"
  short_description: "Execute manual regressions and capture proof"
  default_prompt: "Use $factory-evidence to execute the supplied regression scope and capture sanitized evidence."
policy:
  allow_implicit_invocation: false
