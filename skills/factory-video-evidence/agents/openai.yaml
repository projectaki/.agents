interface:
  display_name: "Factory Video Evidence"
  short_description: "Record one supplied UI workflow as video"
  default_prompt: "Use $factory-video-evidence with the project auth directory to add one supplied UI workflow script and compact review video to the existing evidence run."
policy:
  allow_implicit_invocation: false
