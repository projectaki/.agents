interface:
  display_name: "Factory Video Evidence"
  short_description: "Record one justified UI workflow as video"
  default_prompt: "Use $factory-video-evidence to record one supplied video-required UI workflow in the existing evidence run."
policy:
  allow_implicit_invocation: false
