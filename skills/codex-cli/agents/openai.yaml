interface:
  display_name: "Codex CLI"
  short_description: "Stream one observable Codex CLI task"
  default_prompt: "Use $codex-cli to run one observable read-only Codex task in a subagent and stream its progress."
policy:
  allow_implicit_invocation: false
