interface:
  display_name: "Codex CLI"
  short_description: "Run one isolated Codex CLI task"
  default_prompt: "Use $codex-cli to run one isolated read-only Codex CLI task and return its result."
policy:
  allow_implicit_invocation: false
