interface:
  display_name: "Factory Telemetry"
  short_description: "Record and summarize Factory runtime events"
  default_prompt: "Use $factory-telemetry to record or summarize optional Factory runtime events."
