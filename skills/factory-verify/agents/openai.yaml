interface:
  display_name: "Factory Verify"
  short_description: "Prove every changed behavioral path"
  default_prompt: "Use $factory-verify with the current implementation review and change-assurance report to prove every changed path and produce a PR-ready confidence report."
policy:
  allow_implicit_invocation: false
