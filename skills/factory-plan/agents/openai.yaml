interface:
  display_name: "Factory Plan"
  short_description: "Turn context into an implementation-ready plan"
  default_prompt: "Use $factory-plan with the supplied change context and test-scope packet to create an implementation-ready plan."
policy:
  allow_implicit_invocation: false
