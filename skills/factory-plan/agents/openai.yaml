interface:
  display_name: "Factory Plan"
  short_description: "Turn context into an implementation-ready plan"
  default_prompt: "Use $factory-plan to turn the supplied context into a detailed plan for an implementor agent."
policy:
  allow_implicit_invocation: false
