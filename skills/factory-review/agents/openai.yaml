interface:
  display_name: "Factory Review"
  short_description: "Review any supplied lifecycle subject"
  default_prompt: "Use $factory-review to review the supplied subject against its stated criteria."
policy:
  allow_implicit_invocation: false
