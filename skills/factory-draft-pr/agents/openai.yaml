interface:
  display_name: "Factory Draft PR"
  short_description: "Publish a path-assured draft PR"
  default_prompt: "Use $factory-draft-pr to publish the verified change with complete diff accountability and durable path-level evidence."
policy:
  allow_implicit_invocation: false
