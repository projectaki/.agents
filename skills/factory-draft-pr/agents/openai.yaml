interface:
  display_name: "Factory Draft PR"
  short_description: "Publish an evidence-backed draft PR"
  default_prompt: "Use $factory-draft-pr to publish the verified change as an evidence-backed draft PR."
policy:
  allow_implicit_invocation: false
