interface:
  display_name: "Factory Orchestrator"
  short_description: "Run or resume a guarded Factory task"
  default_prompt: "Use $factory-orchestrator to restore this task and run its next guarded Factory lifecycle."
policy:
  allow_implicit_invocation: false
