interface:
  display_name: "Factory Orchestrator"
  short_description: "Route and run the next Factory step"
  default_prompt: "Use $factory-orchestrator to restore this task and run its next valid lifecycle step."
policy:
  allow_implicit_invocation: false
